name: my_llm_app
description: A new Flutter project

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  groq_sdk: ^0.0.5 # Check pub.dev for the latest version
  flutter_dotenv: ^5.1.0